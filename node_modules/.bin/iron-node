#!/bin/sh
basedir=$(dirname "$(echo "$0" | sed -e 's,\\,/,g')")

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../iron-mocha/bin/iron-mocha" "$@"
  ret=$?
else 
  node  "$basedir/../iron-mocha/bin/iron-mocha" "$@"
  ret=$?
fi
exit $ret
